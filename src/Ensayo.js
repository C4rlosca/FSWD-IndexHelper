import Data from "./db/data.json";

const Ensayo = () => {
  return (
    <h2
      style={{
        color: "white",
        textAlign: "center",
        fontSize: "3rem",
      }}
    >
      Hello
    </h2>
  );
};

export default Ensayo;
